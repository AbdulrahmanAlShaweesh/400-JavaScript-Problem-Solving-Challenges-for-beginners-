// get an array of words
// use join to return a string
// add a space between the words
// return the smashed sentance
function sentenceSmashed(array) {
  return array.join(' ').trim();
}
console.log(sentenceSmashed(["Hello", "World", "is", "best", "place", "to", "leave", "in"]))

// other way using